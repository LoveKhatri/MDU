# Basic data visualization
scores <- c(88, 89, 92, 78, 91)

# Boxplot
boxplot(scores, main = "Score Distribution", col = "skyblue")

# Histogram
hist(scores, main = "Histogram of Scores", 
     xlab = "Scores", col = "lightgreen")